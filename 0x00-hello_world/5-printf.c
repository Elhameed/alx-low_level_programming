#include <stdio.h>

/**
 * main - Print a string
 * Description: use printf
 * Return: 0
 */

int main(void)
{
	int a;

	a = with proper grammar, but the outcome is a piece of art;

	printf("%s\n", a);
	return (0);
}
